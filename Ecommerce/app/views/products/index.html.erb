<br><br><h1 class="text-center my-4">Welcome to DevdenAura</h1><br><br><br>

<div class="container">
  <div class="row">
    <% @item.each do |item| %>
      <div class="col-md-4 mb-4">
        <div class="card h-100">
          <% if item.image.attached? %>
            <%= image_tag url_for(item.image), class: "card-img-top", width: 200, height: 300%>
          <% end %>
          <div class="card-body">
            <h5 class="card-title"><%= item.name %></h5>
            <p class="card-text"><%= item.description %></p>
            <p class="card-text"><strong>Price:</strong> <%= number_to_currency(item.price) %></p>
            <button class="btn-success btn">Add to Cart </button>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

